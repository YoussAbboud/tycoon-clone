{
  "name": "fresh-squeeze",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Fresh Squeeze — a lemonade stand tycoon game (TypeScript + Vite, Canvas 2D)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit",
    "fast": "node --experimental-strip-types scripts/fast.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
